[package]
name = "aether-ffi"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
repository = "https://github.com/knarayanareddy/forge"
publish = false

[lib]
name = "aether_ffi"
crate-type = ["staticlib", "cdylib"]

[dependencies]
# Phase 4: FFI exports are thin C ABI hints only; agent execution stays on TCP IPC.
