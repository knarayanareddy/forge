;; AetherForge Tool Sandbox Profile (sandbox_tool.sb)
(version 1)
(allow default)

(deny network*)

(deny file-write*
    (require-not (subpath (param "WORKSPACE_PATH"))))

(deny file-read*
    (subpath "/etc")
    (subpath "/private/etc"))
